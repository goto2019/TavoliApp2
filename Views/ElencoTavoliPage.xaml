<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="TavoliApp.Views.ElencoTavoliPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:viewmodels="clr-namespace:TavoliApp.ViewModels"
    xmlns:models="clr-namespace:TavoliApp.Models"
    x:DataType="viewmodels:ElencoTavoliViewModel"
    Title="Elenco Tavoli">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Logout" Clicked="OnLogoutClicked" />
    </ContentPage.ToolbarItems>

    <ContentPage.Content>
        <Grid Padding="10">
            <CollectionView x:Name="TavoliCollection"
                            ItemsSource="{Binding Tavoli}"
                            SelectionMode="Single"
                            SelectionChanged="CollectionView_SelectionChanged">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:TavoloDto">
                        <Frame BorderColor="Gray" CornerRadius="10" Padding="10" Margin="5">
                            <VerticalStackLayout>
                                <Label Text="Tavolo: {Binding NumeroTavolo}" FontAttributes="Bold" FontSize="16" />
                                <Label Text="Stato: {Binding Stato}" />
                                <Label Text="Totale: € {Binding Totale}" />
                            </VerticalStackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </Grid>
    </ContentPage.Content>
</ContentPage>
